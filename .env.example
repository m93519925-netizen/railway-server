# ==========================================
# RAILWAY SERVER — KHÔNG commit file .env
# ==========================================

# AI Provider
AI_API_KEY=sk-Bvd9rSR06Rtroz8NCvTKJA
AI_API_URL=https://llm.thesparkdaily.com/v1/chat/completions
AI_MODEL=deepseek-v4-flash

# Bí mật ký HMAC — phải giống INTERNAL_SECRET trên Vercel
# Tạo bằng: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
INTERNAL_SECRET=replace-with-64-byte-hex-secret

# Origin Vercel được phép gọi vào
ALLOWED_VERCEL_ORIGIN=https://your-app.vercel.app

# Cổng Railway tự cấp (thường không cần đặt tay)
PORT=4000
